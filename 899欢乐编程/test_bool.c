#include <stdio.h>

int main(){
    int b = 0;
    if ( !b ) {
        printf("b代表不成立!");
    } else {
        printf("b代表成立!");
    }
    /*
    int a = 0;
    scanf("%d", &a);
    if (a == 0) {
        printf("a是0\n");
    } else {
        printf("a不是0\n");
    }
    printf("a: %d\n", a);
    */
}
